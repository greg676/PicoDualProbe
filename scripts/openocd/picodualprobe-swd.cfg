# PicoDualProbe — OpenOCD SWD config
# For debugging ARM Cortex-M targets (RP2040, STM32, etc.)
#
# Usage: openocd -f scripts/openocd/picodualprobe-swd.cfg -f target/rp2040.cfg

adapter driver cmsis-dap
transport select swd
adapter speed 4000
